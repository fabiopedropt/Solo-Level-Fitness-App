<View style={styles.logoContainer}>
  {/* Try to load the image, fallback to the component */}
  {require('../assets/solo-leveling-logo.png') ? (
    <Image
      source={require('../assets/solo-leveling-logo.png')}
      style={styles.logo}
      resizeMode="contain"
    />
  ) : (
    <SoloLevelingLogo />
  )}
</View>